export const defaultErrorTitle = 'Date Error!';
export const defaultErrorMessage = `Expiration date can't be set before creation date and can't be equal!`;

export const dateFormat = 'dd.MM.yyyy HH:mm';

export const dateSortOption = 'date';
export const textSortOption = 'text';
export const ascendOrder = 'ascend';
export const descendOrder = 'descend';
